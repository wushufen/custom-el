{
  "name": "web-component-ui-library",
  "version": "1.0.0",
  "description": "A UI component library based on Web Components",
  "main": "src/index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.js --bundle --minify --drop:console --target=es2024 --inject:./src/globals.js --outfile=dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "web-components",
    "ui-library"
  ],
  "author": "",
  "license": "ISC"
}
